> ⚠️ Устарело (май 2026, Windows-этап проекта). Описывает более раннюю версию
> архитектуры (v4.1/v4.2 scoring, путь c:/Users/_r3drum/...). С тех пор проект
> переехал на Mac, scoring обновился до v5 (добавлен vocal_character), и многие
> пункты из ПЛАН_ДАЛЬШЕ.md уже реализованы. Актуальное состояние смотри в
> памяти Claude / SESSION_NOTES.md, не здесь.

---

# План дальнейшей разработки VoxBridge

Что **ещё не сделано** или запланировано после текущего MVP (май 2026).

---

## Бэкенд и инфраструктура

- [ ] **Supabase** (PostgreSQL, auth, storage) — только по явному запросу пользователя; сейчас всё в localStorage.
- [ ] **Production deploy** Python-сервиса (Docker, GPU-опция, отдельный хост) и Next.js (Vercel/свой VPS).
- [ ] **Персистентность** embeddings / кэш демо вокалистов (не пересчитывать ECAPA каждый раз).
- [ ] **Аутентификация** API voice-match (сейчас открытый localhost endpoint).
- [ ] **Очередь задач** для длинных треков (Celery/RQ) вместо 30 s partial на CPU.
- [ ] Demucs на **демо** (сейчас только AI vocal) — опционально, trade-off скорость/качество.
- [ ] **GPU** inference для Demucs/ECAPA в проде.
- [ ] Поле **`manual_gender` в form-data** (сейчас только `MANUAL_DEMO_GENDER` в коде).

---

## AI / matching quality

- [ ] Подключить **реальный matching** к публичному flow `/search` → `/results` (сейчас mock в `lib/matching.ts`).
- [ ] **Оптимизация моделей**: квантизация, onnx, меньший Demucs, batch embeddings.
- [ ] Убрать или спрятать **debug-поля** в production API:
  - `final_ranking_score`, `gender_priority_tier`, `prototype_pitch_timbre_similarity`, внутренние флаги partition.
- [ ] Калибровка scoring на реальном датасете (A/B тесты с продюсерами).
- [ ] **Lyrics / language** — явно вне scope ECAPA; не обещать match по тексту.
- [ ] Обучение собственной модели — **не в плане MVP**.

---

## Frontend / продукт

- [ ] Реальная **загрузка аудио** в профиль вокалиста (сейчас mock file names / players).
- [ ] Stripe / оплата (`/order/create` — stub).
- [ ] Замена **legacy routes** (`/upload`, `/matches`, stub admin) или удаление.
- [ ] Синхронизация **AGENTS.md** с актуальными маршрутами (`PROJECT_MEMO.md` новее).
- [ ] i18n: вынести русские строки AI Lab в словарь, если нужен EN.
- [ ] E2E тесты AI Lab + mock API.

---

## Admin AI Lab (полировка)

- [ ] Сохранение последних прогонов matching в localStorage / admin DB.
- [ ] Экспорт результатов CSV.
- [ ] Batch upload многих демо с прогресс-баром.
- [ ] Показ `partial: true` пользователю явным баннером.
- [ ] Health-check индикатор backend в UI (сейчас ошибка только при Run).

---

## DevOps / Windows

- [ ] CI: `npm run build`, `py_compile main.py`, sanity scoring.
- [ ] Документировать минимальный **размер бэкапа** (pretrained_models ~сотни MB).
- [ ] `.env.example` в корне с `NEXT_PUBLIC_VOICE_MATCH_API_URL`.
- [ ] Добавить `pretrained_models/` в `.gitignore` + инструкция download — если не хотите хранить веса в git.

---

## Явно не делать без запроса

- Supabase, Stripe, real auth
- Переписывание всего `HomeWorkspace` / `InternalShell`
- `useSyncExternalStore` для stores
- Force push в main

---

## Приоритеты (рекомендация)

1. Сохранить стабильный dev loop (proxy scripts, pretrained_models в бэкапе).
2. Подключить voice-match к `/results` с feature flag.
3. Supabase + real uploads — когда пользователь попросит.
4. Production deploy voice service.

---

*Сверяться с [`ПРОГРЕСС_ПРОЕКТА.md`](./ПРОГРЕСС_ПРОЕКТА.md) при закрытии пунктов.*
